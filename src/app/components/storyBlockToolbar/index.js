import ng from 'angular';
import storyBlockToolbarComponent from './storyBlockToolbar.component';

export default ng.module('app.components.storyBlockToolbar', [])
  .component('storyBlockToolbar', storyBlockToolbarComponent)
  .name;
