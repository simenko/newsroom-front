import ng from 'angular';
import storyToolbarComponent from './storyToolbar.component';

export default ng.module('app.components.storyToolbar', [])
  .component('storyToolbar', storyToolbarComponent)
  .name;
